Specification specB

Imports:
	import "specC.spear"

Inputs:
	in : int

Outputs:
	out : int
	
State:
	local : int

Macros:
	m1 : int = local + in

Assumptions:

Requirements:
	r0: local == spec specC(0)
	r1: out == spec specC(m1)

Properties:
	p0: out >= 0

